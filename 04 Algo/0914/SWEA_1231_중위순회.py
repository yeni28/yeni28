def inorder(n):
    if n != 0:
        inorder(cl[n])
        print(word([n], end='')
        inorder(cr[n])


for tc in range(1,11):
    N = int(input())
    cl = [0] * (N+1)
    cr = [0] * (N+1)
    word = [0] * (N+1)

    for _ in range(N):
        list1 = list(input().split())
        node = int(list1[0])
        wd = list[]

        if len(list1) >= 4:
            



    print(f'#{tc}, end=""')
    inorder(1)
    print()